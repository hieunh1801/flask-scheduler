# flask-scheduler
Flask integrate scheduler
Tích hợp scheduler vào vào trong flask
### script
```bash
# create venv
virtualenv venv

# install lib
pip install -r requirement.txt

# run 
python app.py
```